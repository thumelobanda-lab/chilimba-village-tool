# Cloudflare Pages SPA fallback — keeps any deep link or reload landing
# on index.html at the edge rather than 404ing, even though the app is a
# single route today (no client-side pathname routing).
/*    /index.html   200
